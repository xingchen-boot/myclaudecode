# 项目介绍
本项目是一个AI终端应用项目，可在终端启动，辅助用户进行开发，类似于claude code

# 项目技术栈
使用nodejs配合node的一些库进行开发，请求大模型接口用的是openai。项目开发使用的js，不是用ts

# 项目的目录结构
[./src]("项目的代码存储处")
[./src/app.js]("项目的启动文件,通过运行该文件启动项目")
[./src/docs]("项目携带给大模型接口的文档模板放在这里")
[./src/tool]("项目本地的functiontool到时候读取tools里面的内容给到大模型接口")
[./src/utils]("项目代码使用的工具函数放在这里")

#项目的额外规范
1. 项目使用的是esmodule规范，不是commonjs规范
2. 项目使用js开发，不是用ts
3. 注意扫描package.json，如果有没有安装的包，请使用npm install安装依赖
4. 每次生成的代码都必须要添加注释，注释要中文
5. 禁止使用语义化和可读性差的变量名等
6. 每次生成代码的时候如果当前目录没有一个更新文档的md文件，就创建一个，如果有就更新这次进行了哪些修改